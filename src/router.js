import Home from '@views/Home';

const routes = [
    {
        name: 'main',
        path: '/',
        component: Home
    },
    {
        name: 'home',
        path: '/home',
        component: Home
    }
];

export default routes;
